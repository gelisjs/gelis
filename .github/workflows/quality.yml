name: Quality

on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: Format, typecheck, and tests
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Show authoritative Prettier diff
        run: |
          bunx prettier bench/secure-headers/p11-f8-enabled-acceptance.mts bench/secure-headers/p11-f8-http-server.mts --write
          git diff -- bench/secure-headers/p11-f8-enabled-acceptance.mts bench/secure-headers/p11-f8-http-server.mts
