name: CP4-AS Compact Collision Candidate

on:
  push:
    branches:
      - work/competitive-cp4as-compact-collision-index
    paths:
      - .github/workflows/cp4as-compact-collision-candidate.yml

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: work/competitive-cp4as-compact-collision-index

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Generate candidate
        run: python3 scripts/cp4as-compact-collision-patch.py

      - name: Format and validate
        run: |
          bunx prettier --write src/runtime/router.ts test/runtime/router-secondary-fingerprint.test.ts
          bun test test/runtime/router-secondary-fingerprint.test.ts
          bun run check

      - name: Commit candidate and clean temporary generator
        run: |
          rm .github/workflows/cp4as-compact-collision-candidate.yml
          rm scripts/cp4as-compact-collision-patch.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/runtime/router.ts test/runtime/router-secondary-fingerprint.test.ts .github/workflows/cp4as-compact-collision-candidate.yml scripts/cp4as-compact-collision-patch.py
          git commit -m "perf(router): replace duplicate collision map with compact secondary index"
          git push origin HEAD:work/competitive-cp4as-compact-collision-index
