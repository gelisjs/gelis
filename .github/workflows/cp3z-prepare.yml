name: CP3-Z Prepare

on:
  push:
    branches:
      - work/competitive-cp3z-post-promotion-dynamic-residual

permissions:
  contents: write

jobs:
  prepare:
    if: github.event.head_commit.message == 'CP3Z_PREPARE'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: work/competitive-cp3z-post-promotion-dynamic-residual
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.2
      - run: bun install --frozen-lockfile
      - name: Format CP3-Z files
        run: |
          bunx prettier --write \
            bench/runtime/cp3z-post-promotion-dynamic-residual-worker.mts \
            bench/runtime/cp3z-post-promotion-dynamic-residual-acceptance.mts \
            docs/benchmarks/competitive-cp3z-post-promotion-dynamic-residual-freeze.md
          if ! git diff --quiet; then
            git config user.name github-actions[bot]
            git config user.email 41898282+github-actions[bot]@users.noreply.github.com
            git add bench/runtime/cp3z-post-promotion-dynamic-residual-worker.mts \
              bench/runtime/cp3z-post-promotion-dynamic-residual-acceptance.mts \
              docs/benchmarks/competitive-cp3z-post-promotion-dynamic-residual-freeze.md
            git commit -m "chore(competitive): format CP3-Z harness"
            git push origin HEAD:work/competitive-cp3z-post-promotion-dynamic-residual
          fi
      - name: Full quality
        run: bun run check
      - name: CP3-Z correctness probe
        run: bun bench/runtime/cp3z-post-promotion-dynamic-residual-acceptance.mts --probe-only
      - name: Verify source guard
        run: git diff --quiet af4e5102046def1b163435333563b8d08f919bf5 HEAD -- src
