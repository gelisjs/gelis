name: CP4-X Harness Freeze

on:
  push:
    branches:
      - work/competitive-cp4x-generic-table-request-specialization
    paths:
      - .cp4x-harness-trigger

permissions:
  contents: write

jobs:
  freeze:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: work/competitive-cp4x-generic-table-request-specialization
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.2
      - run: bun install --frozen-lockfile
      - name: Generate permanent CP4-X artifacts
        run: python .github/scripts/cp4x_harness_generate.py
      - name: Format permanent artifacts
        run: bunx prettier --write bench/runtime/cp4x-generic-table-request-specialization-acceptance.mts bench/runtime/cp4x-generic-table-request-specialization-worker.mts docs/benchmarks/competitive-cp4x-generic-table-request-specialization-freeze.md
      - name: Full quality
        run: bun run check
      - name: Commit frozen artifacts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add bench/runtime/cp4x-generic-table-request-specialization-acceptance.mts bench/runtime/cp4x-generic-table-request-specialization-worker.mts docs/benchmarks/competitive-cp4x-generic-table-request-specialization-freeze.md
          git commit -m "bench(competitive): freeze CP4-X balanced acceptance"
          git push origin HEAD:work/competitive-cp4x-generic-table-request-specialization
