name: CP4-AJ Correctness Probe

on:
  push:
    branches:
      - work/competitive-cp4aj-lazy-kind-primary-viability
    paths:
      - .cp4aj-probe-trigger

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
        with:
          ref: work/competitive-cp4aj-lazy-kind-primary-viability
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.2
      - run: bun install --frozen-lockfile
      - name: Run CP4-AJ correctness probe only
        run: bun bench/runtime/cp4aj-lazy-kind-primary-viability.mts --probe-only
