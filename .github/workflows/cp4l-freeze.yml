name: CP4-L Freeze

on:
  push:
    branches:
      - work/competitive-cp4l-static-capability-elision
    paths:
      - .cp4l-freeze-trigger

permissions:
  contents: write

jobs:
  freeze:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: work/competitive-cp4l-static-capability-elision
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.2
      - run: bun install --frozen-lockfile
      - name: Generate CP4-L artifacts
        run: python .github/scripts/cp4l_freeze.py
      - name: Format artifacts
        run: bunx prettier --write bench/runtime/cp4l-static-capability-elision-acceptance.mts bench/runtime/cp4l-static-capability-elision-worker.mts docs/benchmarks/competitive-cp4l-static-capability-elision-freeze.md
      - name: Full quality
        run: bun run check
      - name: Commit frozen CP4-L artifacts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add bench/runtime/cp4l-static-capability-elision-acceptance.mts bench/runtime/cp4l-static-capability-elision-worker.mts docs/benchmarks/competitive-cp4l-static-capability-elision-freeze.md
          git diff --cached --quiet && exit 0
          git commit -m "bench(competitive): freeze CP4-L static capability elision"
          git push origin HEAD:work/competitive-cp4l-static-capability-elision
