name: CP4-AO Correctness Probe

on:
  push:
    branches:
      - work/competitive-cp4ao-all-only-production-acceptance
    paths:
      - .cp4ao-probe-trigger

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
        with:
          ref: work/competitive-cp4ao-all-only-production-acceptance
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.2
      - run: bun install --frozen-lockfile
      - name: Run CP4-AO correctness probe only
        run: bun bench/runtime/cp4ao-all-only-production-acceptance.mts --probe-only
