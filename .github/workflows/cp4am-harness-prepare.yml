name: CP4-AM Harness Prepare

on:
  push:
    branches:
      - work/competitive-cp4am-ak-inline-all-fallback-viability
    paths:
      - .cp4am-harness-trigger

permissions:
  contents: write

jobs:
  prepare:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: work/competitive-cp4am-ak-inline-all-fallback-viability
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.2
      - run: bun install --frozen-lockfile
      - name: Build frozen CP4-AM viability harness
        run: python .github/scripts/cp4am_build_viability_harness.py
      - name: Format frozen artifacts
        run: bunx prettier --write bench/runtime/cp4am-ak-inline-all-fallback-viability.mts bench/runtime/cp4am-ak-inline-all-fallback-viability-worker.mts docs/benchmarks/competitive-cp4am-ak-inline-all-fallback-viability-freeze.md
      - name: Full quality
        run: bun run check
      - name: Commit frozen harness
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add bench/runtime/cp4am-ak-inline-all-fallback-viability.mts bench/runtime/cp4am-ak-inline-all-fallback-viability-worker.mts docs/benchmarks/competitive-cp4am-ak-inline-all-fallback-viability-freeze.md
          git commit -m "bench(cp4am): freeze AK inline ALL fallback viability"
          git push origin HEAD:work/competitive-cp4am-ak-inline-all-fallback-viability
