name: CP4-AW Shared Collision Param Candidate

on:
  push:
    branches:
      - work/competitive-cp4aw-shared-collision-param-name
    paths:
      - .github/workflows/cp4aw-shared-collision-param-candidate.yml

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: work/competitive-cp4aw-shared-collision-param-name

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Generate candidate
        run: python3 scripts/cp4aw-shared-collision-param-patch.py

      - name: Format and validate
        run: |
          bunx prettier --write src/runtime/router.ts
          bun test test/runtime/router-secondary-fingerprint.test.ts
          bun run check

      - name: Commit candidate and clean temporary generator
        run: |
          rm .github/workflows/cp4aw-shared-collision-param-candidate.yml
          rm scripts/cp4aw-shared-collision-param-patch.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/runtime/router.ts .github/workflows/cp4aw-shared-collision-param-candidate.yml scripts/cp4aw-shared-collision-param-patch.py
          git commit -m "perf(router): share collision trailing parameter name"
          git push origin HEAD:work/competitive-cp4aw-shared-collision-param-name
