name: CP4-V Correctness Probe

on:
  push:
    branches:
      - work/competitive-cp4v-balanced-composition-attribution
    paths:
      - .cp4v-probe-trigger

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: work/competitive-cp4v-balanced-composition-attribution
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.2
      - run: bun install --frozen-lockfile
      - name: CP4-V correctness probe only
        run: bun bench/runtime/cp4v-balanced-composition-attribution.mts --probe-only
